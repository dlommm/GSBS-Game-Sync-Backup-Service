version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - govet
    - staticcheck
    - ineffassign

issues:
  exclude-rules:
    # QF* codes are cosmetic refactoring suggestions, not bugs
    - linters: [staticcheck]
      text: "^QF"
