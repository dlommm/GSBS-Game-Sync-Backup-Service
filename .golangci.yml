version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - govet
    - staticcheck
    - ineffassign
  exclusions:
    rules:
      # QF* codes are cosmetic refactoring suggestions, not bugs
      - linters: [staticcheck]
        text: "^QF"
