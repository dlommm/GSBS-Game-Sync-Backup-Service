events {}

http {
	upstream gsbs {
		server gsbs-server:8080;
	}

	server {
		listen 80;
		server_name gsbs.example.com;

		location / {
			proxy_pass http://gsbs;
			proxy_set_header Host $host;
			proxy_set_header X-Real-IP $remote_addr;
			proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
			proxy_set_header X-Forwarded-Proto $scheme;
		}
	}

	# Uncomment after obtaining TLS certificates (e.g. certbot --nginx)
	# server {
	# 	listen 443 ssl http2;
	# 	server_name gsbs.example.com;
	# 	ssl_certificate /etc/letsencrypt/live/gsbs.example.com/fullchain.pem;
	# 	ssl_certificate_key /etc/letsencrypt/live/gsbs.example.com/privkey.pem;
	# 	location / {
	# 		proxy_pass http://gsbs;
	# 		proxy_set_header Host $host;
	# 		proxy_set_header X-Real-IP $remote_addr;
	# 		proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
	# 		proxy_set_header X-Forwarded-Proto $scheme;
	# 	}
	# }
}
