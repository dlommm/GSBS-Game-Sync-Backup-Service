# Screenshots

Placeholder images are checked in for README layout. Replace with real captures before a marketing release:

| File | Capture |
|------|---------|
| `dashboard.png` | WebUI dashboard after login |
| `admin-overview.png` | `/admin` overview page |
| `tray-menu.png` | Windows or Linux systray menu |
| `setup-wizard.png` | Client first-run setup (`http://127.0.0.1:41234`) |

Optional: `sync-demo.gif` — tray status changing after a save upload.

Keep PNGs under ~500 KB (compress with `pngquant` or similar).
